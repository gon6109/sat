<UserControl x:Class="SatUI.Property"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SatUI"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid Background="{DynamicResource {x:Static SystemColors.AppWorkspaceBrushKey}}">
        <Expander x:Name="expander" Header="Expander" VerticalAlignment="Center" HorizontalContentAlignment="Stretch" FontSize="14" IsExpanded="True">
            <Grid Background="#FFE5E5E5">
                <StackPanel x:Name="PropertyItems" Margin="5,0,0,0" Grid.Row="1"/>
            </Grid>
        </Expander>

    </Grid>
</UserControl>
